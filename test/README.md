# Test assets

Shared directory fixtures for `backend/core` integration tests.

- `fixtures/flat/` — top-level-only sample files
- `fixtures/nested/` — nested files that LooseRootScan must skip
- `fixtures/symlinks/` — reserved for unix symlink edge cases
